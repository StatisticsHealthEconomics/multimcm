# Make_latent_model_code

Make_latent_model_code

## Usage

``` r
make_latent_model_code(model, id = 1L)
```

## Examples

``` r
if (FALSE) { # \dontrun{
lapply(make_latent_model_code("lognormal"), cat)
} # }
```
