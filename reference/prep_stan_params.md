# Default prior latent variable

Default parameters for survival model priors if not supplied.

## Usage

``` r
default_prior_latent(formula_latent, formula_cure)
```

## Arguments

- formula_latent:

  Formula for latent

- formula_cure:

  Formula for cure fraction

## Value

hyper-parameters
