# Posterior prediction Stan code builder

Posterior prediction Stan code builder

## Usage

``` r
create_stancode_postpred(
  models = c("exp", "weibull", "gompertz", "loglogistic", "lognormal")
)
```
